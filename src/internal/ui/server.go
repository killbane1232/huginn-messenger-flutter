package ui

// This package is replaced by bridge.go in the root package.
// All FFI exports for Flutter are now in src/bridge.go (package main).
// Build with: go build -buildmode=c-shared -o libhuginn_messenger.so
